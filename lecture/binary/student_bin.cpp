class Student {
    private:

};
// no converdion needed because th data type is already character
// how any byts do yoy kneed to wrtite? lenName;
// cqn do size of int wehich just gives the max amt of bytes of an int to br sble to rad it but typicallt you want ot just do the size of th thing itself 

// ypu dont need (char*) conversion if the thg we are triying to write or rad 
ofstream fout("student.o", ios::binary){

    }