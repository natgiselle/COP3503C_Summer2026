#include "functions.h" // tells the compiler the header and source file are connected (grabs most recent version and add to the top ofo your file.cpp)
#include <iostream> // implements input output stream standard library

int main() {

}