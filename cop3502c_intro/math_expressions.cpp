/* MATHEMATICAL EXPRESSIONS:

*/

#include <string>
#include <iostream>


int main() {
    int a = 10;
    int b = 3;
    int c = a / b;
    

    // division with 2 ints = int (floating point number)
    // division with an int and float = float (keeps full value)
    
    int x = 10; 
    // int y = x++; // post-fix x: current x = 10 in y and then x is incremented by 1 so x= 11
    std::cout << y;
    int z = ++x; // adds value to x first x = 11 z = 11
    //  value of x is incremeented by 1 so x is 11 and then the result is stored in z at the end 
    std::cout << x;
    return 0;
}

